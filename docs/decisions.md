Design Decisions — daaram-pwa

형식: 날짜 | 결정 | 이유 한 문장. 한 결정 = 한 줄. Claude와 석 달 뒤의 나에게 주는 가장 싼 컨텍스트.

2026-07-10 | 스택을 Next.js(App Router) + TS + Tailwind + Zustand + Dexie(IndexedDB)로 확정, 서버 DB 없음 | 지인 1인 전용 확정(동기화 불필요) + 무료 우선 제약 + 기존 자산(타입/유틸/스토어 구조) 이식 비용 0
2026-07-10 | LLM/프롬프트 관련 하네스 부품(전용 클라이언트 인터페이스, 프롬프트 경로 가드)을 두지 않음 | 이 프로젝트는 LLM을 쓰지 않음 — 근거 없는 부품은 만들지 않는다
2026-07-10 | 프로젝트 고유 위협 F5~F8(백업손실/IndexedDB마이그레이션/캐시오염/접근게이트우회)을 하네스에 반영 | 서버 없이 클라이언트(브라우저)에만 데이터를 두는 구조라 생기는 특유의 위협들
2026-07-10 | verify-gate/post-edit을 단일 workspace 구조로 설계 | 백엔드가 없는 Next.js 단독 repo라 워크스페이스 분리 실익이 없음
2026-07-10 | 접근 제어는 middleware + 공유 비밀번호 1개로, 풀 인증 시스템은 도입하지 않음 | 사용자 1인 확정 상황에서 계정/OAuth는 과설계 — 마찰 실측 전 예방적 증설 금지
2026-07-10 | 백업 파일 암호화는 MVP 범위에서 하지 않음 | 파일이 지인 본인 기기 밖으로 나갈 구체적 경로가 없어 위협 근거 약함, 필요해지면 그때 추가
2026-07-10 | git 커밋/브랜치/푸시는 Claude 자동 실행 없이 사람이 직접 수행 | diff를 사람이 매번 눈으로 거치는 관문을 유지하고 싶음 — 속도보다 안전판 우선
2026-07-10 | 브랜치 전략은 main 직진행, 실험적 시도에만 예외적으로 브랜치 사용 | 협업자 없음 + verify-gate가 이미 부서진 상태 완료선언을 막음 + P1 단계가 순차 의존이라 병렬 브랜치 필요성 자체가 없음
2026-07-10 | CostLineItem.quantityGram을 원시 number 대신 NonNegativeNumber branded type으로 감쌈 | 원시 타입 노출 금지 원칙을 도메인 수량 값에도 일관 적용, 음수 수량을 타입 단계에서 차단
2026-07-10 | importBackup에 forceEmpty 옵션 추가, 테이블이 기존에 레코드가 있는데 신규 백업이 0개로 줄이는 경우 기본적으로 거부 | F5(백업이 지인의 유일한 데이터 사본을 덮어씀) 위험 — 대량 소실을 명시적 확인 없이 자동 반영하지 않기 위함
2026-07-10 | verify-gate.sh의 head_count/skip_count grep 범위를 app/lib/store에서 저장소 전체(node_modules 등 제외)로 확장 | Next.js 16의 proxy.ts/proxy.test.ts가 레포 루트에 있어야 하는데 기존 범위가 루트 파일을 못 봐서 "테스트 개수 감소" 거짓 게이밍 경보가 발생함
2026-07-10 | known gap: IndexedDB read 경로(db.ts/store) zod 미검증, 추후 조치 필요 | 현재 스토어가 Dexie 조회 결과를 zod 재검증 없이 바로 state에 반영함 — CLAUDE.md의 "IndexedDB에서 읽은 값도 신뢰 경계" 원칙과 어긋나지만 이번 엔티티 재정의 범위 밖이라 스키마만 재사용 가능하게 옮겨두고 실제 적용은 보류
2026-07-11 | lib/infra/repository.ts(제네릭 CRUD 팩토리)로 위 known gap을 해소, store/*.ts도 Dexie 직접 호출 대신 리포지토리 경유로 교체 | list()는 오염 레코드를 skippedCount로 건너뛰고(단일 레코드 손상이 전체 목록을 막지 않도록), get()은 명시적으로 하나를 요청한 것이므로 CorruptedRecord로 실패시켜 "삭제됨"과 "깨짐"을 구분함
2026-07-11 | 게이트 쿠키를 SHA-256 해시(무만료) 방식에서 HMAC-SHA256 서명 + exp 클레임(30일) 방식으로 교체(lib/infra/gate.ts), 서명 키는 별도 env 없이 GATE_PASSWORD 재사용, 실패 시 고정 지연(700ms)만 적용하고 IP 기반 레이트리밋은 도입하지 않음 | 무만료 해시 쿠키는 탈취 시 영구 접근을 허용함 + 서명 위조에도 결국 비밀번호가 필요해 별도 시크릿의 이득이 적음 + 지인 1인 위협 모델에서 상태 저장형 레이트리밋은 과설계
2026-07-11 | 레시피 CRUD 구현 시 Recipe 엔티티에 재료 구성(lines)을 두지 않고, lines의 유일한 원천을 RecipeVersion.snapshotJson으로 한정함(recipes 테이블 스키마 변경 없음) | 저장할 때마다 새 버전 스냅샷이 자동 생성되는 요구와 자연히 맞아떨어지고, recipes 테이블에 lines를 추가했다면 IndexedDB v3 마이그레이션과 백업 스키마 버전업이 함께 필요했을 것 — 근거 없는 스키마 변경을 피함
2026-07-11 | 버전 "복원"은 과거 데이터를 덮어쓰지 않고 선택한 스냅샷을 편집 폼 상태로만 불러옴 — 저장을 눌러야 새 버전(versionNo 최댓값+1)으로 append됨 | 이력을 append-only로 유지해 실수로 과거 버전을 삭제/훼손할 수 없게 함
2026-07-11 | 레시피 에디터 UI는 jsdom/@testing-library 없이(신규 의존성 승인 없이) vitest 자동 테스트 대상에서 제외하고 /verify로 브라우저에서 직접 확인함 | vitest.config.ts가 environment:"node"라 RTL 도입은 별도 승인 필요 사항 — 이번 범위에서 새 의존성을 추가하지 않기로 함
2026-07-11 | 재료 CRUD 구현 시 categoryId는 계속 null 고정, IngredientCategory 관리 UI를 만들지 않음(레시피 categoryId 결정 연장) | 카테고리 CRUD가 이번 요구사항(재료 CRUD·가격이력·재고표시·공급업체)에 없어 근거 없는 범위 확장을 피함
2026-07-11 | 공급업체 삭제 시 참조 무결성(그 공급업체를 쓰는 재료가 있어도) 강제하지 않음 | supplierId가 nullable이고 이 레포 전체에 FK 강제 사례가 없어(레시피 삭제도 daily_checklist를 정리하지 않음) 선례를 따름 — 필요해지면 그때 추가
2026-07-11 | RecipeCategory/IngredientCategory/PackageUnit 라벨 CRUD를 createLabelStore(제네릭 팩토리) + LabelManager(제네릭 UI) 하나씩으로 3종 공용 구현, 각각 별도 스토어/컴포넌트 파일로 만들지 않음 | 세 라벨 타입이 {id,name,colorHex}로 구조가 완전히 동일하고(entities.schema.ts의 labelSchema<B>() 팩토리와 동일 근거), lib/infra/repository.ts의 createRepository 제네릭화 선례를 그대로 따름
2026-07-11 | PackageUnit을 Ingredient.stockUnit의 FK로 바꾸지 않고 자유입력 + datalist 자동완성으로만 연결 | v1→v2 마이그레이션에서 이미 Ingredient.packageUnitId가 제거된 상태였고, FK로 강제하려면 엔티티 스키마 변경(IndexedDB v3 마이그레이션)이 필요해 근거 없는 스키마 변경을 피함 — 이전 결정(재료 categoryId 미노출)은 이번 작업으로 뒤집혀 Recipe/Ingredient 모두 categoryId 선택 UI가 생김
2026-07-11 | 오늘 생산 체크리스트에 요구사항에 없던 removeChecklistItem(삭제)을 추가함 | 잘못 추가한 항목을 지울 방법이 없으면 실사용이 막힘 — 레시피/재료/공급업체/라벨 등 지금까지 만든 모든 목록 기능에 삭제가 있었던 선례를 따름
2026-07-11 | DailyChecklist에 createdAt이 없어 같은 날짜 내 항목 정렬 순서를 보장하지 않음(Dexie 기본 반환 순서에 의존) | 스키마 변경(필드 추가) 없이 가기로 결정 — 순서가 실제로 문제 되면 그때 필드 추가 + 마이그레이션 논의
2026-07-11 | 백업 export/import UI(app/backup/page.tsx) 추가 시 lib/infra/backup.ts의 exportBackup/importBackup 핵심 로직(zod 검증·버전체크·대량소실방지·트랜잭션 전량반영)은 이미 구현·테스트돼 있어 그대로 두고, 브라우저 다운로드(triggerBackupDownload)·파일읽기(readBackupFileAsJson) 헬퍼 2개만 추가함 [F5] | 재구현하면 이미 검증된 롤백 보장을 건드릴 위험이 있음 — 새로 추가한 회귀 테스트로 "뒤쪽 테이블 쓰기 실패 시 앞쪽 테이블까지 전부 롤백"을 직접 확인함(backup.test.ts)
2026-07-11 | importBackup이 트랜잭션 자체 실패(예: bulkAdd 제약 위반) 시 Result가 아니라 예외를 던진다는 걸 발견했으나 backup.ts는 수정하지 않고, app/backup/page.tsx의 호출부만 try/catch로 감싸 대응함 [F5] | 이미 검증된 backup.ts 핵심 로직을 계획 승인 범위 밖에서 수정하지 않기 위함 — Result로 통일하려면 별도 계획·승인 필요
2026-07-11 | PNG 아이콘(app/icon.tsx, app/apple-icon.tsx)을 next/og의 ImageResponse로 생성(새 의존성 없음), 기존 public/icons/icon.svg와 동일 디자인(배경 #4f46e5, 흰 "다") 재사용 | iOS Safari가 apple-touch-icon에 SVG를 지원하지 않아 PNG가 필수 — 별도 디자인 도구나 사람이 만든 로고 없이 Next.js 내장 기능으로 해결
2026-07-11 | sw.js fetch 핸들러에 /_next/static/* 런타임 캐싱(캐시-우선 + 최초요청 시 채워넣기)을 추가, install/activate 로직과 APP_SHELL_URLS는 그대로 둠, CACHE_VERSION을 v1→v2로 올림 [F7] | 기존 SW는 3개 정적 경로만 캐싱해서 오프라인 재실행 시 JS 청크 요청이 네트워크로 나가 실패하는 상태였음 — 해시된 불변 파일이라 버전 걱정 없이 영구 캐시 가능, CACHE_VERSION 안 올리면 구버전 사용자가 새 캐싱 로직을 못 받음
2026-07-11 | /icon, /apple-icon 라우트를 sw.js의 APP_SHELL_URLS(설치 시 프리캐시)에 추가하지 않음 | 두 라우트가 proxy.ts 매처의 공개 예외(icons 문자열)와 다른 이름(icon)이라 실제로는 게이트에 걸림 — 로그인 전 SW가 최초 설치되면 게이트 리다이렉트 응답으로 캐시가 오염될 위험이 있어 추가하지 않음
2026-07-11 | known gap: 로그인 전(게이트 통과 전) 상태에서 서비스워커가 최초 설치되면 "/"이 게이트 리다이렉트 응답으로 캐시될 가능성이 있음(사전부터 있던 상태, 이번 작업으로 발견) | RegisterServiceWorker가 루트 레이아웃에 있어 /gate 페이지에서도 SW 등록이 실행됨 — 이번 MVP 마감 작업 범위 밖이라 고치지 않음, docs/pwa-install-check.md에 알려진 한계로 기록. 고치려면 게이트 통과 후에만 SW를 등록하거나 "/"을 프리캐시 목록에서 빼는 방안 검토 필요
2026-07-12 | 디자인 토큰(브랜드/재료/데이터/위험/가격하락 accent+soft 배경)을 tailwind.config 없이 app/globals.css의 @theme 블록 한 곳에만 정의, 화면별 hex 하드코딩 금지 | Tailwind v4가 CSS-first 설정(tailwind.config 파일 부재 확인)이라 @theme이 유일한 토큰 정의 지점 — bg-brand/border-danger 등 유틸리티가 자동 생성되어 화면 코드에서 임의 hex를 쓸 이유가 없어짐
2026-07-12 | Badge의 colorHex→배경/글자색 산출(lib/domain/labelColor.ts)에서 배경 lightness=94%, 글자 lightness=25%로 고정(원본 hue/saturation은 유지)하고 WCAG AA 대비(4.5:1) 이상을 vitest로 검증 | 요청서에 정확한 수치가 없어 브랜드 예시(#C2185B → soft #FCE7F0)를 HSL로 역산해 94%가 가장 근접한 값임을 확인, 대표 hue 5종에서 실제 대비비(5.7~11.8) 계산해 임의 hex 입력에도 가독성이 보장됨을 확인 후 채택
2026-07-12 | components/ui/(PageHeader, Card, Badge, FilterChip, SearchBar, EmptyState, ConfirmDialog)를 구축하되 기존 화면(레시피/재료/공급업체/체크리스트/설정)에는 이번 라운드에서 리팩터링 적용하지 않음 | 스코프를 "공용 컴포넌트 구축"으로 한정해 한 턴의 diff 크기를 통제 — 화면별 적용은 후속 작업으로 분리하기로 사용자와 확인
2026-07-12 | app/backup/page.tsx의 window.confirm 2곳(가져오기 전체 대체 확인, RequiresConfirmation 재확인)만 ConfirmDialog로 교체, recipes/ingredients 목록의 확인 없는 즉시삭제 버튼은 이번엔 손대지 않음 [F5] | 원 요청이 "기존 window.confirm 사용처 통일"로 한정되어 있었고 사용자가 삭제 버튼 확장은 별도 작업으로 남기기로 확인 — import 로직(zod 검증·트랜잭션·대량소실방지)은 그대로 두고 confirm 트리거만 동기(window.confirm)에서 상태 기반 비동기(ConfirmDialog) 흐름으로 교체
2026-07-12 | 하단 탭바(app/BottomTabBar.tsx)를 components/ui/가 아닌 app/에 register-sw.tsx와 같은 방식으로 배치 | 이 컴포넌트는 앱의 실제 라우트(/recipes, /checklist 등)를 하드코딩으로 아는 앱 셸 조각이라 재사용 가능한 범용 UI 프리미티브가 아님
2026-07-12 | 하단 탭바의 활성 탭 강조를 탭별 도메인 accent(brand/ingredient/data)가 아닌 단일 brand accent로 통일 | 홈/설정 탭에 자연스럽게 대응되는 도메인 토큰이 없어 탭별 색 분기 시 임의 배정이 필요했음 — iOS 탭바 관례(탭마다 같은 틴트 컬러)를 따르는 것이 더 일관적이라 사용자와 확인 후 단일 accent로 결정
2026-07-12 | /gate 페이지에서도 main의 탭바용 하단 패딩(calc(--tab-bar-height + safe-area-inset-bottom + 1.5rem))을 조건 없이 그대로 둠 | /gate는 폼 하나뿐인 페이지라 여백이 남아도 무해한 반면, 조건부 제거를 하려면 body에 경로를 아는 별도 클라이언트 wrapper가 필요해 이득 대비 복잡도가 커 사용자가 직접 현재 방식 유지로 확정
2026-07-12 | 탭 활성 판정(isTabActive)을 lib/domain/navigation.ts 순수함수로 분리 — href="/recipes"가 /recipes/new, /recipes/[id] 같은 하위 경로에서도 활성 유지되도록 슬래시 경계 기반 prefix 매칭 구현 | 정확 일치만 하면 상세/생성 페이지에서 탭이 꺼져 보여 실사용성이 떨어짐 — 동시에 /recipes-archive 같은 문자열만 겹치는 경로는 오탐되지 않아야 해서 경계 로직에 전용 테스트(lib/domain/navigation.test.ts)를 둠
2026-07-12 | components/ui/(PageHeader, Card, Badge, EmptyState, ConfirmDialog)를 레시피/재료/공급업체/체크리스트/설정(LabelManager) 9개 화면에 실제로 적용, FilterChip/SearchBar는 이번에도 제외 | 대응되는 기존 필터·검색 기능이 전혀 없어 "적용"이 아니라 "신규 기능 추가"가 되므로 디자인 시스템 적용과 분리 — 필요해지면 별도 요청으로 스펙을 다시 잡기로 함
2026-07-12 | confirm 없이 즉시삭제되던 5곳(레시피/재료/공급업체 목록, 체크리스트 항목, 라벨관리) 전부에 ConfirmDialog 추가, 각 화면에 로컬 `useState<T\|null>(pendingDelete)`를 개별로 둠(공용 훅으로 추출하지 않음) | 지난 세션엔 backup 2곳만 교체하고 미뤄뒀던 확장 — 5곳 모두 확인 문구/엔티티 타입이 달라 훅 추출 이득이 적어 "비슷한 코드 3~4줄이 섣부른 추상화보다 낫다" 원칙을 적용
2026-07-12 | 레시피/재료 목록·에디터에 카테고리 Badge를 신규로 추가(기존엔 목록·에디터 어디에도 카테고리 자체가 표시되지 않았음), LabelManager 각 행에도 입력 중인 name/colorHex를 실시간 반영하는 Badge 미리보기 추가 | Badge 컴포넌트의 존재 의의(엔티티 colorHex를 실제로 보여주는 것)를 살리려면 "표시 자체가 없던" 지점에 새로 추가하는 것이 불가피하다고 판단 — 스타일 교체를 넘는 신규 UI지만 원 요청("배지... colorHex 사용")의 의도에 부합한다고 보고 포함
2026-07-12 | Card는 최상위 목록 페이지(레시피/재료/공급업체/체크리스트/라벨관리)에만 적용하고 에디터 내부 중첩 목록(재료라인·원가·가격이력·버전이력)은 이번 스코프에서 제외 | 중첩 목록까지 포함하면 diff가 크게 늘어나 한 턴에 검토하기 어려워짐 — 사용자와 확인 후 범위를 최상위 목록으로 한정
2026-07-12 | 홈 화면(app/page.tsx)을 단순 nav 링크 목록에서 브랜드 헤더+인사말+오늘생산 진행률 카드+바로가기 그리드로 전면 재작성, PageHeader는 재사용하지 않고 헤더 마크업을 직접 작성 | PageHeader의 필수 뒤로가기 버튼 계약이 탭 루트인 홈 화면과 맞지 않음(홈에서 "뒤로"는 의미가 없음) — 하위 페이지 전용 컴포넌트를 억지로 재사용하지 않기로 함
2026-07-12 | 브랜드 로고는 public/brand/indigo-gelato-heart.png(요청됨) 대신 실제로 존재하는 indigo-gelato-heart.jpg를 그대로 사용 | 요청한 파일 확장자가 저장소에 없었고, 사용자가 확인 질문에서 기존 .jpg 그대로 쓰기로 확정함
2026-07-12 | app/checklist/page.tsx의 로컬 todayDateString()을 lib/domain/date.ts로 추출해 홈 화면과 공유 | 오늘 생산 카드가 같은 "오늘 날짜" 문자열을 필요로 하게 되어 2번째 사용처가 생김 — 로직 변경 없이 위치만 이동, 형식(YYYY-MM-DD) 검증 테스트 1개 추가
2026-07-12 | 오늘 생산 카드의 빈 상태에서 기존 EmptyState 컴포넌트를 그대로 쓰고 "메뉴 추가하기" 버튼은 그 아래 별도 Link로 배치, EmptyState 자체에 버튼 슬롯을 추가하지 않음 | 버튼이 필요한 사용처가 아직 이 화면 하나뿐이라 공용 컴포넌트 인터페이스를 넓히는 것은 과설계로 판단 — 필요해지면 그때 슬롯을 추가
2026-07-12 | 레시피 목록의 "재료 N개" 표시를 위해 recipe_versions 테이블을 목록 페이지에서도 bulk 로드하기로 함(store/recipeStore.ts에 versions와 분리된 allVersions/loadAllVersions 신설) | "레이아웃 재구성" 요청 범위를 넘어서는 데이터 로딩이라 사용자에게 확인 후 진행 — 기존 versions/loadVersions(상세 페이지의 단일 레시피 이력용)와 같은 필드를 쓰면 목록↔상세 이동 시 서로 다른 목적의 데이터가 덮어써지는 충돌이 생겨 분리함
2026-07-12 | 레시피별 최신 버전 선정 로직(latestVersionByRecipeId)을 lib/domain/recipeVersion.ts에 순수함수로 추가, 재료 개수 자체는 기존 parseRecipeSnapshot을 그대로 재사용(새 파싱 로직 없음) | 최신 버전 선정에 versionNo 비교라는 실질 분기가 있어 vitest로 검증할 가치가 있다고 판단 — 스냅샷 파싱은 이미 검증된 함수라 재구현하지 않음
2026-07-12 | 레시피 검색+카테고리 필터 AND 조합을 lib/domain/recipeFilter.ts 순수함수(filterRecipes)로 분리, UI에는 if문을 두지 않음 | 요청에 명시된 요구사항 — 검색어 trim+대소문자 무시, categoryId=null("전체")은 카테고리 없는 레시피도 포함하되 특정 카테고리 지정 시엔 제외되는 경계를 테스트로 고정
2026-07-12 | components/ui/PageHeader.tsx에 옵션 subtitle prop을 추가(기존 6개 화면은 미전달 시 렌더링 변화 없음) | 레시피 목록 헤더에 "총 N개" 부제가 필요해져 두 번째 실사용처가 생김 — 하위 호환되는 옵션 확장이라 새 인터페이스를 만드는 것과는 다르다고 판단
2026-07-12 | 레시피 목록의 "새 레시피" 버튼을 사각형 텍스트 링크에서 얇은 brand 테두리의 원형 "+" 버튼으로 교체 | 요청된 "우측 원형 + 버튼" 디자인을 반영하되, 카드/배지에 이미 쓰인 "얇은 보더, 그림자 거의 없음" 톤을 유지
2026-07-12 | 레시피 상세 화면을 신설하며 라우팅을 재편: /recipes/[id]는 읽기 전용 상세(신규 RecipeDetail.tsx), 기존 수정 화면(RecipeEditor)은 /recipes/[id]/edit로 이동, /recipes/[id]/history를 이력 전체보기로 신설 | 상세 요청서가 "상단 뒤로가기 + 수정/삭제 버튼"을 명시해 REST식 상세/수정 분리가 필요했음 — 목록 카드 링크(`/recipes/${id}`)는 코드 변경 없이 자동으로 상세로 연결됨, 사용자 확인 후 진행
2026-07-12 | app/recipes/VersionHistory.tsx의 onRestore/limit prop을 optional로 확장(기존 필수 onRestore 전달 호출부는 동작 불변) | 상세 화면의 "최근 3건 프리뷰"(복원 불가, limit=3)와 이력 페이지(복원 불가, 전체)에서 재사용하기 위함 — 복원은 편집 폼 상태로만 불러오는 개념이라 편집 화면 밖에서는 버튼 자체를 숨김
2026-07-12 | 총 원가 카드의 노란 배경은 새 디자인 토큰을 추가하지 않고 기존 --color-ingredient-soft(#fef3c7)를 재사용, Card 컴포넌트(bg-white 고정) 대신 직접 마크업(rounded-2xl border border-ingredient bg-ingredient-soft)으로 작성 | Card의 bg-white와 배경색 클래스가 유틸리티 특이도 동률로 충돌할 수 있어 재사용을 포기 — 사용자가 새 토큰 추가 대신 기존 토큰 재사용을 확정
2026-07-12 | 레시피 상세 화면의 배치량 스테퍼(±500g, 최소 500g)를 lib/domain/batch.ts에 stepBatchSize 순수함수로 추가하고 vitest(red→green)로 검증, 화면 자체는 2026-07-11 결정(jsdom 미도입)에 따라 계속 /verify 브라우저 확인 대상으로 남김 | 클램프 로직은 순수 계산이라 단위테스트 가치가 있으나 화면 렌더는 기존 결정을 뒤집을 근거가 없음
2026-07-12 | 이번 라운드는 GATE_PASSWORD를 알 수 없어(.env 접근 금지 원칙) /verify 브라우저 구동 확인을 생략하고 tsc/vitest/eslint 통과로 마무리 — known gap | 사용자가 직접 확인하기로 함, 코드 리뷰 시 브라우저 미검증 상태였음을 유의
2026-07-12 | 재료 상세 화면을 신설하며 레시피 상세와 동일 패턴으로 라우팅 재편: /ingredients/[id]는 읽기 전용 상세(신규 IngredientDetail.tsx), 기존 수정 화면(IngredientEditor)은 /ingredients/[id]/edit로 이동 | 목록 카드 링크(`/ingredients/${id}`)는 코드 변경 없이 자동으로 상세로 연결됨 — 직전에 승인된 레시피 상세 라우팅 패턴을 재확인 없이 그대로 적용
2026-07-12 | 재료 목록 검색+카테고리 필터를 lib/domain/recipeFilter.ts와 제네릭으로 합치지 않고 lib/domain/ingredientFilter.ts에 구조가 동일한 filterIngredients를 별도로 둠 | 로직은 동일하지만(검색어 trim+대소문자 무시, categoryId=null이면 미분류 포함) 제네릭화하려면 이번 요청 범위 밖인 recipeFilter.ts까지 건드려야 함 — "비슷한 코드 3~4줄이 섣부른 추상화보다 낫다" 원칙(2026-07-12 앞선 결정)과 동일 판단
2026-07-12 | 가격 변동 이력의 "가격"과 변동폭은 g당 단가(pricePerGram)가 아니라 통가격(packagePrice) 기준으로 계산(computePriceDeltas, lib/domain/ingredientPricing.ts에 추가) | "+가격 등록" 모달이 "통가격만 입력(포장량 유지)"으로 명시돼 있어 사용자가 실제로 입력하는 값과 이력에 표시되는 값을 일치시킴 — 사용자 확인 후 확정
2026-07-12 | 가격 변동 방향에 스펙에 없던 "flat"(가격 불변) 상태를 ▲(up)/▼(down)/최초(first) 3종 외에 추가하고 UI에서는 "-"로 표시 | hasPackagePriceChanged가 실제 저장 경로에서 무변동 시 새 이력을 안 만들지만, 과거 데이터(백업 복원 등)에는 동일 가격이 연속으로 남아있을 수 있어 up/down으로 잘못 분류되는 것을 막기 위한 방어적 케이스
2026-07-12 | "+가격 등록" 모달(PriceEntryModal.tsx)은 saveIngredient를 그대로 호출해 packagePrice만 새 값으로, 나머지 필드는 현재 재료 값을 그대로 전달 — 새 저장 경로나 별도 zod 스키마를 만들지 않음 | 가격이력 자동 기록(hasPackagePriceChanged 트리거)과 기존 검증 로직을 그대로 재사용하기 위함, 요청서의 "새 저장 경로를 만들지 마라" 제약을 그대로 반영
2026-07-12 | PriceEntryModal의 open 상태를 prop으로 받지 않고 IngredientDetail에서 `{priceModalOpen && <PriceEntryModal .../>}`로 조건부 마운트 | open prop 방식은 모달이 열릴 때마다 useEffect에서 packagePrice/errorMessage를 리셋해야 해 "setState in effect" 안티패턴(eslint react-hooks/set-state-in-effect)에 걸림 — 조건부 마운트로 매번 새 useState 초기값을 쓰게 해 effect 없이 해결
2026-07-12 | app/ingredients/PriceHistory.tsx가 packagePrice/packageAmount를 함께 보여주던 것을 packagePrice + 델타 표시로 교체(packageAmount 표시는 제거) — 이 컴포넌트는 IngredientEditor에도 재사용되고 있어 그 화면의 가격이력 표시도 함께 바뀜 | 상세 화면 요청("날짜+가격, 우측에 직전 대비 변동폭")을 그대로 반영한 결과이며, 기준(g)은 상세 화면의 별도 정보표 행으로 이미 노출되므로 정보 중복이 아님
2026-07-12 | 공급업체 화면(/suppliers 목록·에디터)과 "확인 없는 즉시삭제 버튼 전체 교체"는 grep으로 재확인한 결과 이미 2026-07-12 앞선 라운드에서 완료돼 있어 이번 작업에서 코드 변경 없이 확인만 하고 마침 | PageHeader/Card/EmptyState/ConfirmDialog가 이미 적용돼 있고 앱 전체에 확인 없는 삭제 버튼이 하나도 없음을 직접 확인함
2026-07-12 | 카테고리 색상 프리셋 10종(CATEGORY_COLOR_PRESETS)을 lib/domain/labelColor.ts에 상수로 추가 — app/globals.css @theme 디자인 토큰으로 넣지 않음 | 이 값들은 앱 자체 UI 크롬 색(브랜드/재료/데이터 accent)이 아니라 사용자가 자신의 라벨에 고를 수 있는 색상 데이터라서 "화면별 hex 하드코딩 금지" 토큰 규칙의 적용 대상이 아니라고 판단 — vitest로 정확한 10개 값·형식을 고정해 오타를 방지함
2026-07-12 | /settings를 "카테고리 관리" 카드 1개짜리 허브로 축소하고, 기존에 한 페이지에 나열되던 3개 LabelManager 섹션을 /settings/categories(3카드 진입)와 /settings/categories/{recipe,ingredient,package-unit} 3개 서브 라우트로 분리 | 요청서가 "/settings/categories: 3개 카드로 분기"를 명시했고, 카드 accent는 기존 Card 토큰(brand/ingredient/data)을 그대로 재사용해 새 디자인 토큰 없이 핑크/옐로우/블루 요구를 만족시킴
2026-07-12 | 카테고리 타입별 서브 라우트를 동적 라우트(/settings/categories/[type]) 대신 recipe/ingredient/package-unit 3개 정적 파일로 둠 | 타입마다 서로 다른 Zustand 훅(useRecipeCategoryStore 등)을 쓰고 있어 동적 매핑을 만들면 오히려 간접화만 늘어남 — store/labelStores.ts가 이미 3개를 명시적으로 나열하는 선례를 따름
2026-07-12 | LabelManagerProps.title을 필수에서 optional로 확장(생략 시 내부 h2 미출력, 기존 3개 스택 사용처는 title을 계속 넘겨 동작 불변) | 새 서브 라우트들은 PageHeader가 이미 페이지 제목을 보여주므로 LabelManager 내부 h2까지 있으면 제목이 중복 표시됨
2026-07-12 | ColorSwatchPicker의 "직접 선택" 스와치를 별도 장식 버튼이 아니라 `<input type="color">` 자체를 원형으로 스타일링해 구현, 그 옆에 hex 텍스트 input을 항상 나란히 배치(토글 없음) | "input type=color + hex 직접 입력 병행"이라는 요청 문구를 그대로 반영 — 프리셋 미일치 시 이 원형 컬러 input에 선택 테두리를 줘 "직접 선택" 상태를 표시
2026-07-12 | LabelRow의 색상 편집을 상시 노출 대신 "색상 변경" 버튼으로 토글되는 인라인 ColorSwatchPicker로 바꿈(AddLabelRow는 토글 없이 항상 노출) | 등록된 항목은 화면 공간을 아끼기 위해 기본 접힘, 신규 추가 폼은 어차피 매번 색을 골라야 해 바로 노출하는 게 자연스럽다고 판단 — 스펙엔 명시 없던 세부 결정이라 계획에도 가정으로 표시했음
2026-07-12 | /settings/categories 진입 카드에 각 타입의 등록 개수("N개") 부제를 추가 | 스펙엔 없었지만 3개 스토어를 이미 로드해야 해 비용이 낮고, 진입 전 상태 파악이라는 화면 목적에 부합한다고 판단해 계획에 가정으로 명시 후 진행
2026-07-12 | "모든 데이터 삭제" 기능(wipeAllData, lib/infra/backup.ts)을 새 table.clear() 로직 없이 buildEmptyBackupFile()+기존 importBackup(forceEmpty:true) 재사용으로 구현 [F5] | CLAUDE.md Never 목록의 "IndexedDB 전체 삭제 코드는 승인 없이 추가 금지" 대상 기능 — 이 계획 승인이 그 승인 절차이며, 이미 트랜잭션 원자성·롤백이 테스트로 검증된 경로를 그대로 타 새 취약점을 만들지 않음. UI는 2단계 ConfirmDialog + 1단계 문구에 "삭제 전에 백업하기를 먼저 하세요" 고정 포함. 회귀 테스트(wipeAllData가 9개 테이블 전부 비우는지)를 backup.test.ts에 추가함
2026-07-12 | 설정 화면의 "백업하기"는 exportBackup+triggerBackupDownload를 인라인 재호출(신규 로직 없음), "백업에서 복원"은 /backup으로 Link만 연결 — app/backup/page.tsx와 lib/infra/backup.ts의 기존 export/import 함수 본문은 전혀 수정하지 않음 [F5] | 가져오기는 파일선택+검증+ConfirmDialog 체인이 이미 완성돼 있어 그대로 재사용하는 게 안전, 내보내기만 원탭 단축을 위해 두 번째 호출부를 추가
2026-07-12 | /checklist의 날짜 선택 `<input type="date">`를 제거하고 오늘(todayDateString)에 고정 | 재디자인 스펙의 헤더가 "타이틀 + 오늘 날짜"만 명시해 날짜 브라우징은 이번 화면 목적(오늘 생산) 밖이라고 판단 — 과거/미래 조회가 필요해지면 별도 화면으로 분리하는 편이 나음
2026-07-12 | 체크리스트 상태 배지 색상을 새 토큰 없이 기존 토큰으로 매핑(대기=중립 회색 텍스트, 진행중=data, 완료=price-down)하고 배경은 셋 다 bg-gray-100으로 통일 | price-down 토큰은 원래 "가격 하락"용이지만 초록색이 "완료/성공"의미로도 자연스러워 재사용 — 새 soft 배경 토큰을 추가하지 않기 위해 배경은 중립 회색 하나로 고정
2026-07-12 | app/checklist/AddChecklistItemModal.tsx를 app/ingredients/PriceEntryModal.tsx와 공용 컴포넌트로 묶지 않고 별도로 작성, 대신 "open prop 없이 조건부 마운트" 패턴은 동일하게 재사용 | 레시피 목록에서 고르는 2단계 흐름(선택→배치량 확정)과 재료 1개의 가격만 입력하는 1단계 흐름은 시그니처가 달라 억지 통합은 과잉설계 — setState-in-effect eslint 문제를 피하는 마운트 패턴만 선례를 따름
2026-07-12 | app/icon.tsx·app/apple-icon.tsx의 "다" 임시 도형을 public/brand/indigo-gelato-mascot.jpg로 교체, node:fs/promises로 파일을 읽어 base64 data URI로 ImageResponse에 임베드하고 export const runtime="nodejs" 추가 | next/og의 ImageResponse 기본(Edge) 런타임엔 fs가 없어 로컬 파일을 읽으려면 Node 런타임 명시가 필요 — 새 의존성 없이 Node 내장 모듈만 사용. public/icons/icon.svg(동일한 "다" 임시 도형)는 요청 범위 밖이라 안 건드림 — manifest.json의 icons 배열에 남아있어 완전한 통일은 아직 아님(후속 과제로 남김)
2026-07-12 | public/manifest.json의 name/short_name을 "다아람"→"Indigo Gelato"로 변경하면서 public/sw.js의 CACHE_VERSION을 v2→v3로 올림 [F7] | manifest.json이 sw.js의 캐시우선 프리캐시 목록(APP_SHELL_URLS)에 있어 버전을 안 올리면 이미 설치된 사용자가 새 이름을 영영 못 받음
2026-07-12 | 남은 toLocaleString() 미적용 표시 숫자(app/page.tsx의 오늘생산 진행률, app/recipes/page.tsx의 배치량)를 통일 — 체크리스트 화면의 나머지 위반(진행률, 항목 배치량)은 이번 라운드의 /checklist 재디자인 자체로 함께 해결됨 | "화면별 hex 하드코딩 금지"와 별개로 이번에 새로 요청된 "숫자는 toLocaleString() 통일" 규칙을 잔여 위반 없이 전면 적용
2026-07-12 | 전체 화면 목업 리디자인 — 공유 컴포넌트(Card/PageHeader/FilterChip/SearchBar/EmptyState) 개편 + Button·SectionTitle·theme.ts 신설로 톤(brand/ingredient/data) 체계를 한 곳에 집중, 각 페이지는 조합만 | globals.css의 element 기본 스타일(input/select/button/section/h2)은 편집기·공급업체 등 미대상 페이지가 의존하므로 그대로 두고, 대상 화면은 컴포넌트/명시 클래스로만 스타일링해 회귀 위험을 차단
2026-07-12 | 라벨 편집 행(LabelManager)의 이름 변경·저장 UI를 "색상 변경"을 누르면 열리는 패널 안으로 이동 | 목업의 접힌 행은 배지·색상변경·삭제만 노출 — 이름 변경 기능(기존 요구)은 유지하되 접힌 상태를 목업과 일치시키기 위해 편집 컨트롤을 패널로 접음
2026-07-12 | 레시피·재료 목록 카드에서 인라인 삭제 버튼을 제거(삭제는 상세 화면에서만) + 재료 상세 현재가 표기를 pricePerGram원/g→packagePrice원/packageAmountg로 변경 | 목업 카드가 이름+배지+요약만 보여주는 클린 카드이고 상세 화면에 삭제가 이미 있어 기능 손실 없음 — 현재가 표기도 목업의 "50,000원/100g" 포맷에 맞춤(데이터는 기존 필드 재사용)
2026-07-13 | 홈/설정의 브랜드 이미지를 next/image→순수 <img>로 교체(+eslint-disable no-img-element), 이에 맞춰 sw.js CACHE_VERSION v3→v4 [F7] | next.config·sharp 부재로 /_next/image 최적화기가 null 반환("valid image 아님") — 정적 장식 이미지라 최적화 불필요하므로 optimizer 경로 자체를 제거. 홈("/")이 SW에 cache-first로 프리캐시돼 구 HTML(구 최적화 URL 참조)이 계속 서빙되므로 버전 범프로 캐시 강제 무효화
2026-07-13 | app/register-sw.tsx: NODE_ENV!=="production"일 때 서비스워커를 등록하지 않고 기존 등록을 unregister하도록 변경 [F7] | 개발 중 SW가 "/"를 cache-first로 잡아 코드 변경이 옛 HTML에 가려지는 문제(브랜드 이미지 디버깅 때 재현)를 원천 차단 — 오프라인 캐싱은 프로덕션 빌드에서만 필요하므로 dev 등록을 끄는 것이 안전
2026-07-14 | 레시피 수정이력 페이지: 전용 복원 액션을 새로 만들지 않고 기존 saveRecipe에 옛 스냅샷(batchSize+lines)+현재 name/category/memo를 넣어 "복원"을 새 버전 생성으로 구현 | 스냅샷 복원은 곧 "그 내용으로 다시 저장"이므로 별도 스토어 액션은 과잉 — 이력이 선형으로 쌓이는 기존 모델과도 일치(현재 내용은 새 버전으로 보존). 버전 간 재료 사용량 diff는 lib/domain/recipeVersionDiff.ts 순수함수로 분리(재료 이름 해석은 표현 계층)
2026-07-14 | 오늘생산에 월간 달력 + 과거 생산 이력 조회 추가 — 2026-07-12 "오늘 고정" 결정을 이 기능 한정으로 되살림 | 스키마 변경 없이 기존 daily_checklist의 date 인덱스로 between() 범위 쿼리(listChecklistItemsInRange)만 추가 → [F5]백업·[F6]마이그레이션·[F7]SW 무영향. 달력 계산은 lib/domain/calendar.ts 순수함수(주 시작=일요일)로 분리·테스트. 상태는 기존 items/loadByDate(홈·선택일)는 유지하고 monthItems/loadMonth(달력 점)만 추가해 기존 스토어 변이·테스트 무변경, 변이 후 페이지에서 loadMonth로 점 갱신. 삭제된 레시피 참조는 "삭제된 레시피"로 표기(표현 계층)
2026-07-14 | 레시피/재료 수정이력의 별도 전체보기 페이지(/[id]/history) 제거하고 상세·편집 화면에서 "더 보기" 인라인 펼치기로 통합 | loadVersions/loadPriceHistory가 이력을 전량 메모리에 올려 limit은 표시용일 뿐 → 라우트·왕복은 과함. 무한스크롤은 지연로딩 이점이 없고 하단 고정버튼과 얽혀 비추 → 명시적 더보기/접기 버튼. 복원은 이미 상세 카드에 있어 페이지 고유기능 소멸. VersionList/PriceHistory에 expanded 상태만 추가
2026-07-14 | 재료를 "그램 원가 + 세는단위 재고 + 다리" 모델(B)로 정리: Ingredient에 unitWeightGram(1세는단위당 그램) 필드 추가. 원가=구매가격÷총중량(g), 재고가치=재고×unitWeightGram×원/g(lib/domain stockGrams/stockValueKrw 순수함수) | 스키마 필드 추가지만 Dexie 인덱스 불변→버전업 없이, entities.schema의 ingredientSchema에 unitWeightGram=positiveNumberSchema.default(1)로 레거시 DB읽기·옛 백업[F5]을 한 번에 호환(백업 import가 같은 스키마 공유). "개수" 유령입력 제거, packageAmount=총중량 직접입력. 세는단위="g"이면 다리 자동 1. 편집기/상세/모든 saveIngredient 폼(PriceEntryModal·복원 포함)에 unitWeightGram 전달
2026-07-15 | 레시피·재료 카테고리를 단일(categoryId)→다중(categoryIds 배열)으로 전환 [F5][F6] | IndexedDB version(3)+upgradeToV3로 기존 categoryId→(값?[값]:[]) 물리 마이그레이션, categoryId 인덱스는 쿼리 미사용이라 제거. entities.schema의 recipe/ingredientSchema를 z.preprocess(normalizeCategoryIds)로 감싸 레거시 레코드·옛 백업(categoryId만 있는)을 categoryIds로 자동 정규화(백업 import가 같은 스키마 공유). 필터 UI는 단일선택 유지하되 매칭은 categoryIds.includes로, 편집기는 칩 다중 토글, 상세/목록/피커는 배지 여러 개. TDD로 필터·스키마·폼·업그레이드(v3)·백업 테스트 추가
2026-07-15 | 서비스워커 프리캐시에서 "/"를 제거하고 CACHE_VERSION v4→v5 [F7] | 게이트가 /→/gate로 리다이렉트하는데 cache.addAll이 redirected 응답을 받으면 TypeError로 install 자체가 실패→깨진 SW(ERR_FAILED). 프리캐시는 게이트가 통과시키는(리다이렉트 없는) 정적 자원(/manifest.json·/icons/icon.svg)만 담는다. 버전업으로 activate가 구캐시(app-shell-v4) 삭제+clients.claim→자가치유(브라우저는 sw.js를 SW우회 네트워크로 갱신확인하므로 다음 접속 시 대체). "/"는 원래도 리다이렉트로 프리캐시가 성공한 적이 없어 오프라인 실손실 없음, _next/static 청크 캐싱은 유지. sw.js는 순수 JS라 new Function 하네스로 install/activate를 모킹 SW전역에서 실행해 테스트(sw.test.ts)
2026-07-15 | 서비스워커를 완전 제거 — public/sw.js를 fetch 핸들러 없는 자폭 스크립트로 교체 [F7] | 게이트가 네트워크로 돌아 오프라인이 원래 불가하고 데이터는 브라우저 로컬 DB에 있어 SW 실익이 없는데 게이트-캐시 상호작용 버그만 반복. 단순 삭제는 기존 설치 SW를 방치(깨진 채 갇힘)하므로 자폭 방식: install→skipWaiting, activate→caches 전삭제+registration.unregister+열린 클라이언트 navigate 재로드. 브라우저가 sw.js를 SW우회 네트워크로 갱신확인→자동 자가치유(ERR_FAILED로 갇힌 기기도 페이지 JS 없이 복구). register-sw.tsx는 RegisterServiceWorker→ServiceWorkerCleanup으로, 등록 중단하고 기존 해제+Cache Storage 삭제만(페이지 레벨 이중 안전). Cache Storage만 삭제하고 IndexedDB(레시피·재료)는 불가침. PWA 홈화면 추가는 manifest 기반이라 유지(안드로이드 자동 설치배너만 사라짐). sw.js는 순수 JS라 new Function 모킹 하네스로 자폭 동작 테스트(sw.test.ts)
